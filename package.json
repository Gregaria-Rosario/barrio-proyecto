{
  "name": "thymeleaf-live-reload",
  "scripts": {
    "watch": "gulp watch",
    "build": "gulp build",
    "build-prod": "gulp build --env production"
  },
  "devDependencies": {
    "browser-sync": "^2.26.14",
    "gulp": "^4.0.2",
    "gulp-watch": "^5.0.1"
  }
}
